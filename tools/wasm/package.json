{
  "type": "module",
  "scripts": {
    "build": "esbuild --bundle src/index.ts --outdir=dist --format=esm --splitting --sourcemap --minify"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
